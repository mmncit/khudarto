import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    position: 'absolute',
    bottom: 40,
    width: '100%',
    zIndex: 50,
  },
  container: {
    marginLeft: 20,
    marginRight: 20,
    backgroundColor: '#00CCBB',
    padding: 16,
    borderRadius: 8,
    flexDirection: 'row',
    alignItems: 'center',
  },
});

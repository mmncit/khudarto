import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  basketFontStyle: {
    color: 'white',
    fontWeight: '800',
    fontSize: 18,
    lineHeight: 18,
  },
});
